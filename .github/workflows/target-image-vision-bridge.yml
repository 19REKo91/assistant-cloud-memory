name: target-image-vision-bridge
on:
  push:
    branches: ["main"]
jobs:
  package:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/upload-artifact@v4
        with:
          name: target-deepseek-88c59e
          path: deepseek_20260919_88c59e.png
          if-no-files-found: error
# exact target test
